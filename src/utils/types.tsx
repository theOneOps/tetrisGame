export type TetriminoType = {
    blocks: number[][];
    color: string;
  };

export type positionType = {
    x: number;
    y: number;
}

export type blockColorType = {
    [key: string]: string;
}